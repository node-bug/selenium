This library provides a refined implementation of selenium-webdriver tailored for test automation efficiency.

Prioritizes selectors based on stability and speed.

No need for Explicit Wait Patterns: Leverage the built-in wait utilities of @nodebug/selenium. Ensure every interaction is guarded by an explicit wait for the element's expected state.

fluent api to focus on atomic user actions.

Implements proper setup and teardown logic. Ensures the browser instance is terminated to prevent memory leaks and zombie processes.

Uses descriptive logging for every major action to ensure that failed executions provide a clear "audit trail" of the browser's state.


Provides Static delays using browser.sleep(ms). Uses the polling mechanisms provided by @nodebug/selenium.

Provides support for external config files.

No Unhandled Promises: Ensures all asynchronous WebDriver calls are properly awaited to prevent race conditions.

uses Expert knowledge of the selenium-webdriver specific methods for element location, attribute retrieval, and browser synchronization.

Asynchronous Execution: Provides Mastery of async/await patterns in Node.js for sequential and parallel browser operations.

Resilient Automation: Implements retry logic and smart-waits to handle flakiness caused by network latency or dynamic UI rendering.

Identify unique attributes for target elements.

Wraps complex interactions in fluent API functions to maintain script readability.

Captures and provides errors (e.g., TimeoutError, StaleElementReferenceError).

Optimized selectors and wait durations based on execution performance.

No static sleeps; all waits are condition-based.

Selectors are optimized for stability and performance.

Code follows a clear "Arrange-Act-Assert" structure.

Comprehensive error handling for common Selenium exceptions is implemented.